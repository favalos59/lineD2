let sno; let can;let hr1;let ob;
function setup(){
	row=500;col=600;rectMode(CENTER):
	can=createCanvas(col,row);
	can,position(300,30);can.class("bdd");
	sno=select('#snow');sno.position(40,50);
	sno.class('inpbdd');
	text("10011001",20,100);
	text("01011001",20,130);
}
function draw(){
	ob.move();ob.chbd();ob.display();
	pic.position(ob.pos.x,ob.pos.y);
}
let str;
function tes (){
	let str=inp.value();
	for(let j=0lj<str.length;j++){textSize(30);
	stroke(0,250,0);list[j]=str.charAt(j);
	text(liss[j[,40,40+20*j);
}
paint(cur);
}